name: Keep Render Backend Awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: curl -sf https://synapse-backend-tz3v.onrender.com/api/health || echo "Ping completed"gi